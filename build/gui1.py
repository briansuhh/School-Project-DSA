
# This file was generated by the Tkinter Designer by Parth Jadhav
# https://github.com/ParthJadhav/Tkinter-Designer


from pathlib import Path
from PIL import Image, ImageTk
from time import sleep

# from tkinter import *
# Explicit imports to satisfy Flake8
from tkinter import Tk, Canvas, Entry, Text, Button, PhotoImage, Label

# current_scale = 2  # The current scale factor
current_scale = 1  # The current scale factor
target_scale = 7  # The target scale factor
step = 0.1  # The step size for scaling

OUTPUT_PATH = Path(__file__).parent
ASSETS_PATH = OUTPUT_PATH / Path(r"C:\Users\USER\Downloads\Tkinter-Designer\build\assets\frame1")

# def enlarge_image():
#     global image_label, current_scale

#     current_scale += 1  # Increase the scale factor
#     image_1 = Image.open("assets/frame1/image_1.png")
#     image = image_1.copy()
#     image = image.resize((int(image.width * current_scale), int(image.height * current_scale)))
#     photo = ImageTk.PhotoImage(image=image)

#     image_label.config(image=photo)
#     image_label.image = photo


def enlarge_image():
    global image_label, current_scale

    if current_scale < target_scale:
        current_scale += step
        pic_1 = Image.open("assets/frame1/image_1.png")
        image = pic_1.copy()
        image = image.resize((int(image.width * current_scale), int(image.height * current_scale)))
        photo = ImageTk.PhotoImage(image=image)

        image_label.config(image=photo)
        image_label.image = photo
        window.after(2, enlarge_image)
    


def relative_to_assets(path: str) -> Path:
    return ASSETS_PATH / Path(path)


window = Tk()

window.geometry("620x400")
window.configure(bg = "#FFFFFF")


canvas = Canvas(
    window,
    bg = "#FFFFFF",
    height = 400,
    width = 620,
    bd = 0,
    highlightthickness = 0,
    relief = "ridge"
)

canvas.place(x = 0, y = 0)
canvas.create_rectangle(
    0.0,
    0.0,
    620.0,
    400.0,
    fill="#F6F4C6",
    outline="")

image_image_1 = PhotoImage(
    file=relative_to_assets("image_1.png"))
# image_1 = canvas.create_image(
#     310.0,
#     190.0,
#     image=image_image_1
# )
image_label = Label(window, image=image_image_1, bg="#F6F4C6", )
image_label.place(relx=0.5, rely=0.5, anchor="center")

image_image_2 = PhotoImage(
    file=relative_to_assets("image_2.png"))
# image_2 = canvas.create_image(
#     309.0,
#     190.0,
#     image=image_image_2
# )

image_label2 = Label(window, image=image_image_2, bg="white")
image_label2.place(relx=0.5, rely=0.5, anchor="center")

# for i in range(10):
#     window.after(1000, enlarge_image)   

enlarge_image()

window.resizable(False, False)
window.mainloop()
