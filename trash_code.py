
# # ------------------------------------- Row 1 -------------------------------------
# row1= ctk.CTkFrame(adopt_frame, corner_radius=0, fg_color="transparent")
# # show all the cats available for adoption based on the list of cats in the database
# # image_label = ctk.CTkLabel(row1, text= "", image=cat_high)
# # image_label.pack(side = ctk.LEFT, padx=10, pady=10)

# image_button = ctk.CTkButton(row1, text="", image=cat_high, width=100, height=100, fg_color="transparent", hover_color="grey", command=cat_button_event)
# image_button.pack(side = ctk.LEFT, padx=10, pady=10)

# image_button = ctk.CTkButton(row1, text="", image=cat_high, width=100, height=100, fg_color="transparent", hover_color="grey", command=cat_button_event)
# image_button.pack(side = ctk.LEFT, padx=10, pady=10)

# image_button = ctk.CTkButton(row1, text="", image=cat_high, width=100, height=100, fg_color="transparent", hover_color="grey", command=cat_button_event)
# image_button.pack(side = ctk.LEFT, padx=10, pady=10)

# row1.pack(side = ctk.TOP, padx=10, pady=10)

# # ------------------------------------- Row 2 -------------------------------------
# row2= ctk.CTkFrame(adopt_frame, corner_radius=0, fg_color="transparent")
# # show all the cats available for adoption based on the list of cats in the database

# image_button = ctk.CTkButton(row2, text="", image=cat_high, width=100, height=100, fg_color="transparent", hover_color="grey", command=cat_button_event)
# image_button.pack(side = ctk.LEFT, padx=10, pady=10)

# image_button = ctk.CTkButton(row2, text="", image=cat_high, width=100, height=100, fg_color="transparent", hover_color="grey", command=cat_button_event)
# image_button.pack(side = ctk.LEFT, padx=10, pady=10)

# image_button = ctk.CTkButton(row2, text="", image=cat_high, width=100, height=100, fg_color="transparent", hover_color="grey", command=cat_button_event)
# image_button.pack(side = ctk.LEFT, padx=10, pady=10)

# row2.pack(side = ctk.TOP, padx=10, pady=10)

# # ------------------------------------- Row 3 -------------------------------------
# row3= ctk.CTkFrame(adopt_frame, corner_radius=0, fg_color="transparent")
# # show all the cats available for adoption based on the list of cats in the database

# image_button = ctk.CTkButton(row3, text="", image=cat_high, width=100, height=100, fg_color="transparent", hover_color="grey", command=cat_button_event)
# image_button.pack(side = ctk.LEFT, padx=10, pady=10)

# image_button = ctk.CTkButton(row3, text="", image=cat_high, width=100, height=100, fg_color="transparent", hover_color="grey", command=cat_button_event)
# image_button.pack(side = ctk.LEFT, padx=10, pady=10)

# image_button = ctk.CTkButton(row3, text="", image=cat_high, width=100, height=100, fg_color="transparent", hover_color="grey", command=cat_button_event)
# image_button.pack(side = ctk.LEFT, padx=10, pady=10)

# row3.pack(side = ctk.TOP, padx=10, pady=10)